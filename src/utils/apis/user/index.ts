import { getProfile, deleteProfile, editProfile } from "./api";
import { Profile, EditProfile } from "./types";

export { getProfile, deleteProfile, editProfile };
export type { Profile, EditProfile };
